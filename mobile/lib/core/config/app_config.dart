/// Offline-first public education app — no backend required.
abstract final class AppConfig {
  static const appName = 'ResilientBuild Pakistan';
  static const tagline = 'Choose Location. Build Safe.';
  static const offlineOnly = true;
}
